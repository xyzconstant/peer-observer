[package]
name = "archiver"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../../shared" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
sha2 = "0.10"

[features]
strict = []
nats_integration_tests = []
